:root {
  --page: #090909;
  --surface: #111111;
  --surface-2: #171717;
  --line: rgba(255, 255, 255, 0.11);
  --line-strong: rgba(255, 255, 255, 0.2);
  --text: #f4f2ed;
  --muted: #a9a59d;
  --accent: #f16c3a;
  --radius: 18px;
  --page-width: 430px;
}

* {
  box-sizing: border-box;
}

html {
  scroll-behavior: smooth;
  background: var(--page);
}

body {
  margin: 0;
  min-width: 320px;
  background: var(--page);
  color: var(--text);
  font-family: "Noto Sans KR", -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
  -webkit-font-smoothing: antialiased;
}

body.drawer-open {
  overflow: hidden;
}

img {
  display: block;
  max-width: 100%;
}

a {
  color: inherit;
  text-decoration: none;
}

button,
a {
  -webkit-tap-highlight-color: transparent;
}

button {
  font: inherit;
  color: inherit;
}

.app-shell {
  width: min(100%, var(--page-width));
  min-height: 100vh;
  margin: 0 auto;
  overflow: hidden;
  background:
    radial-gradient(circle at 78% 7%, rgba(241, 108, 58, 0.08), transparent 25%),
    var(--page);
}

.site-header {
  position: relative;
  z-index: 30;
  height: calc(68px + env(safe-area-inset-top));
  padding:
    calc(12px + env(safe-area-inset-top))
    18px
    10px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  border-bottom: 1px solid var(--line);
  background: rgba(9, 9, 9, 0.96);
}

.site-brand {
  display: flex;
  align-items: center;
}

.site-brand img {
  width: 132px;
  height: 42px;
  object-fit: contain;
  object-position: left center;
}

.menu-button {
  position: relative;
  width: 42px;
  height: 42px;
  padding: 0;
  border: 0;
  background: transparent;
  cursor: pointer;
}

.menu-button span {
  position: absolute;
  right: 2px;
  width: 27px;
  height: 1.5px;
  border-radius: 999px;
  background: var(--text);
}

.menu-button span:first-child {
  top: 15px;
}

.menu-button span:last-child {
  top: 25px;
  width: 19px;
}

.mobile-drawer {
  position: fixed;
  top: 0;
  right: max(calc(50% - var(--page-width) / 2), 0px);
  z-index: 80;
  width: min(86vw, 340px);
  height: 100dvh;
  padding:
    calc(20px + env(safe-area-inset-top))
    20px
    calc(24px + env(safe-area-inset-bottom));
  background: #0d0d0d;
  border-left: 1px solid var(--line);
  transform: translateX(105%);
  transition: transform 220ms ease;
}

.mobile-drawer.is-open {
  transform: translateX(0);
}

.drawer-head {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding-bottom: 18px;
  border-bottom: 1px solid var(--line);
  font-size: 13px;
  font-weight: 800;
  letter-spacing: 0.12em;
}

.drawer-close {
  width: 42px;
  height: 42px;
  padding: 0;
  border: 0;
  background: transparent;
  font-size: 32px;
  font-weight: 300;
  cursor: pointer;
}

.drawer-nav {
  display: grid;
  margin-top: 18px;
}

.drawer-nav a {
  min-height: 62px;
  display: flex;
  align-items: center;
  gap: 15px;
  border-bottom: 1px solid var(--line);
  font-size: 18px;
  font-weight: 700;
}

.drawer-nav a span {
  width: 22px;
  color: var(--accent);
  font-size: 11px;
  font-weight: 800;
}

.drawer-backdrop {
  position: fixed;
  inset: 0;
  z-index: 70;
  background: rgba(0, 0, 0, 0.72);
  backdrop-filter: blur(4px);
  -webkit-backdrop-filter: blur(4px);
}

main {
  display: grid;
}

.hero-section {
  position: relative;
  min-height: 430px;
  padding: 46px 20px 24px;
  overflow: hidden;
  border-bottom: 1px solid var(--line);
  isolation: isolate;
}

.hero-section::after {
  content: "";
  position: absolute;
  inset: auto 0 0;
  height: 44%;
  z-index: -1;
  background: linear-gradient(to bottom, transparent, rgba(9, 9, 9, 0.98));
}

.hero-copy {
  position: relative;
  z-index: 4;
  width: 72%;
}

.eyebrow {
  margin: 0 0 14px;
  color: var(--accent);
  font-size: 11px;
  font-weight: 800;
  letter-spacing: 0.14em;
}

.hero-copy h1 {
  margin: 0;
  font-family: Georgia, "Times New Roman", serif;
  font-size: clamp(43px, 13vw, 58px);
  font-weight: 700;
  line-height: 0.9;
  letter-spacing: -0.055em;
}

.hero-description {
  margin: 20px 0 0;
  color: #d5d1c9;
  font-size: 14px;
  line-height: 1.65;
}

.hero-actions {
  display: flex;
  align-items: center;
  gap: 18px;
  margin-top: 24px;
}

.primary-button {
  min-height: 48px;
  padding: 0 17px;
  display: inline-flex;
  align-items: center;
  gap: 15px;
  border-radius: 12px;
  background: var(--text);
  color: #0b0b0b;
  font-size: 14px;
  font-weight: 800;
}

.primary-button span {
  font-size: 18px;
}

.text-button {
  color: var(--muted);
  font-size: 13px;
  font-weight: 700;
  text-decoration: underline;
  text-underline-offset: 5px;
}

.hero-art {
  position: absolute;
  top: 30px;
  right: -58px;
  z-index: 1;
  width: 250px;
  height: 390px;
  opacity: 0.78;
  transform: rotate(2deg);
}

.hero-art::before {
  content: "";
  position: absolute;
  inset: 0;
  z-index: 2;
  background:
    linear-gradient(to right, var(--page) 0%, transparent 42%),
    linear-gradient(to bottom, transparent 64%, var(--page) 100%);
}

.hero-art img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  filter: grayscale(24%) contrast(1.05);
}

.hero-index {
  position: absolute;
  right: 20px;
  bottom: 18px;
  z-index: 5;
  color: rgba(255, 255, 255, 0.34);
  font-size: 9px;
  font-weight: 700;
  letter-spacing: 0.12em;
}

.content-section,
.links-section {
  padding: 27px 18px;
  border-bottom: 1px solid var(--line);
}

.section-heading {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 14px;
  margin-bottom: 17px;
}

.section-heading > div {
  display: flex;
  align-items: baseline;
  gap: 10px;
}

.section-number {
  color: var(--accent);
  font-size: 10px;
  font-weight: 800;
  letter-spacing: 0.1em;
}

.section-heading h2 {
  margin: 0;
  font-size: 17px;
  font-weight: 800;
  letter-spacing: -0.025em;
}

.section-status,
.section-action {
  color: var(--muted);
  font-size: 10px;
  font-weight: 800;
  letter-spacing: 0.12em;
}

.section-status {
  color: var(--accent);
}

.section-action {
  padding: 2px 0;
  border: 0;
  background: transparent;
  cursor: pointer;
}

.latest-release-wrap {
  display: grid;
  grid-template-columns: 128px minmax(0, 1fr);
  gap: 16px;
  align-items: center;
}

.release-art {
  width: 128px;
  aspect-ratio: 1;
  object-fit: cover;
  border-radius: 14px;
}

.release-copy {
  min-width: 0;
}

.release-tag {
  display: block;
  margin-bottom: 4px;
  color: var(--accent);
  font-size: 10px;
  font-weight: 800;
  letter-spacing: 0.12em;
}

.release-title {
  margin: 0;
  font-family: Georgia, "Times New Roman", serif;
  font-size: 38px;
  line-height: 1;
  letter-spacing: -0.045em;
}

.release-meta {
  margin: 7px 0 0;
  color: var(--muted);
  font-size: 12px;
}

.platform-pills {
  display: flex;
  flex-wrap: wrap;
  gap: 7px;
  margin-top: 17px;
}

.platform-pill {
  min-height: 34px;
  padding: 0 10px;
  border: 1px solid var(--line-strong);
  border-radius: 999px;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  color: #d8d5cf;
  font-size: 11px;
  font-weight: 700;
}

.platform-pill.is-primary {
  border-color: transparent;
  background: var(--text);
  color: #0b0b0b;
}

.works-grid {
  display: grid;
  grid-auto-flow: column;
  grid-auto-columns: 44%;
  gap: 11px;
  overflow-x: auto;
  overscroll-behavior-inline: contain;
  scroll-snap-type: inline mandatory;
  scrollbar-width: none;
}

.works-grid::-webkit-scrollbar {
  display: none;
}

.works-grid.is-expanded {
  grid-auto-flow: row;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  grid-auto-columns: auto;
  overflow: visible;
}

.work-card {
  min-width: 0;
  scroll-snap-align: start;
}

.work-card img {
  width: 100%;
  aspect-ratio: 1;
  object-fit: cover;
  border-radius: 13px;
  background: var(--surface);
}

.work-copy {
  padding-top: 9px;
}

.work-title {
  margin: 0;
  overflow: hidden;
  font-size: 13px;
  font-weight: 800;
  line-height: 1.3;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.work-year {
  margin: 4px 0 0;
  color: var(--muted);
  font-size: 11px;
}

.beats-panel {
  margin: 18px;
  padding: 21px 19px;
  overflow: hidden;
  border: 1px solid var(--line);
  border-radius: var(--radius);
  background:
    radial-gradient(circle at 100% 0%, rgba(241, 108, 58, 0.11), transparent 35%),
    #131313;
}

.beats-topline {
  display: flex;
  align-items: center;
  justify-content: space-between;
  color: var(--muted);
  font-size: 9px;
  font-weight: 800;
  letter-spacing: 0.12em;
}

.beats-topline span:first-child {
  color: var(--accent);
}

.beats-panel h2 {
  margin: 17px 0 0;
  font-family: Georgia, "Times New Roman", serif;
  font-size: 32px;
  line-height: 0.98;
  letter-spacing: -0.04em;
}

.beats-panel p {
  margin: 14px 0 0;
  color: var(--muted);
  font-size: 13px;
  line-height: 1.6;
}

.beats-button {
  min-height: 47px;
  margin-top: 20px;
  padding: 0 15px;
  border-radius: 11px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  background: var(--text);
  color: #0b0b0b;
  font-size: 13px;
  font-weight: 800;
}

.media-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 10px;
  padding: 0 18px 27px;
  border-bottom: 1px solid var(--line);
}

.media-card {
  min-width: 0;
  padding: 14px;
  border: 1px solid var(--line);
  border-radius: 15px;
  background: var(--surface);
}

.media-card-head {
  display: flex;
  align-items: center;
  gap: 8px;
  margin-bottom: 12px;
}

.media-card-head span {
  color: var(--accent);
  font-size: 9px;
  font-weight: 800;
}

.media-card-head strong {
  font-size: 10px;
  letter-spacing: 0.04em;
}

.video-thumbnail {
  position: relative;
  overflow: hidden;
  aspect-ratio: 16 / 10;
  border-radius: 10px;
  background: #222;
}

.video-thumbnail img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  filter: brightness(0.78);
}

.play-badge {
  position: absolute;
  top: 50%;
  left: 50%;
  width: 42px;
  height: 42px;
  border: 1px solid rgba(255, 255, 255, 0.55);
  border-radius: 50%;
  display: grid;
  place-items: center;
  transform: translate(-50%, -50%);
  background: rgba(0, 0, 0, 0.56);
  color: #fff;
  font-size: 14px;
}

.media-card-body p {
  min-height: 39px;
  margin: 11px 0 12px;
  overflow: hidden;
  font-size: 11px;
  font-weight: 700;
  line-height: 1.45;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
}

.outline-button {
  min-height: 38px;
  padding: 0 11px;
  border: 1px solid var(--line-strong);
  border-radius: 9px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  color: #ddd9d2;
  font-size: 10px;
  font-weight: 700;
}

.onyour-wordmark {
  margin: 20px 0 15px;
  overflow: hidden;
  font-family: Georgia, "Times New Roman", serif;
  font-size: clamp(24px, 7vw, 30px);
  letter-spacing: -0.04em;
}

.onyour-card > p {
  min-height: 66px;
  margin: 0 0 14px;
  color: var(--muted);
  font-size: 11px;
  line-height: 1.55;
}

.listen-links {
  display: grid;
  grid-template-columns: 1fr 1fr;
  border-top: 1px solid var(--line);
  border-left: 1px solid var(--line);
}

.listen-link {
  min-height: 52px;
  padding: 0 13px;
  border-right: 1px solid var(--line);
  border-bottom: 1px solid var(--line);
  display: flex;
  align-items: center;
  justify-content: space-between;
  color: #d8d5cf;
  font-size: 12px;
  font-weight: 700;
}

.listen-link b {
  color: var(--accent);
  font-size: 14px;
  font-weight: 500;
}

.listen-link.is-wide {
  grid-column: 1 / -1;
}

.site-footer {
  padding:
    22px
    18px
    calc(22px + env(safe-area-inset-bottom));
  display: flex;
  align-items: center;
  justify-content: space-between;
  background: #060606;
}

.site-footer img {
  width: 106px;
  height: 34px;
  object-fit: contain;
  object-position: left center;
}

.site-footer p {
  margin: 0;
  color: #77736d;
  font-size: 9px;
  letter-spacing: 0.06em;
}

.toast {
  position: fixed;
  left: 50%;
  bottom: calc(22px + env(safe-area-inset-bottom));
  z-index: 120;
  max-width: calc(100vw - 36px);
  padding: 11px 15px;
  border: 1px solid var(--line);
  border-radius: 999px;
  transform: translate(-50%, 8px);
  background: rgba(20, 20, 20, 0.95);
  color: var(--text);
  font-size: 12px;
  text-align: center;
  opacity: 0;
  visibility: hidden;
  transition:
    opacity 150ms ease,
    transform 150ms ease,
    visibility 150ms ease;
}

.toast.is-visible {
  opacity: 1;
  visibility: visible;
  transform: translate(-50%, 0);
}

@media (max-width: 360px) {
  .hero-copy {
    width: 76%;
  }

  .hero-copy h1 {
    font-size: 40px;
  }

  .latest-release-wrap {
    grid-template-columns: 112px minmax(0, 1fr);
    gap: 13px;
  }

  .release-art {
    width: 112px;
  }

  .release-title {
    font-size: 33px;
  }

  .platform-pill {
    padding: 0 8px;
    font-size: 10px;
  }
}

@media (prefers-reduced-motion: reduce) {
  *,
  *::before,
  *::after {
    scroll-behavior: auto !important;
    animation-duration: 0.001ms !important;
    transition-duration: 0.001ms !important;
  }
}
